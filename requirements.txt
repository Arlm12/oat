# OpenAgentTrace Dependencies

# Core SDK (minimal dependencies)
# These are only needed if using the SDK

# Server dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
duckdb>=0.9.2
python-multipart>=0.0.6
websockets>=12.0

# Optional: For auto-instrumentation
openai>=1.3.0
anthropic>=0.7.0
httpx>=0.25.0

# Multimodal media analysis
Pillow>=10.0.0
mutagen>=1.47.0
# opencv-python>=4.8.0  # Optional, for video analysis

# Development
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Note: The OAT SDK itself has no required dependencies
# It only uses Python standard library (sqlite3, json, uuid, etc.)
